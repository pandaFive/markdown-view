# Multi Review Report

## Scope

- 対象: `81981b2347ae1c44f0bb3a03ff28175e4576c80a..HEAD`
- 対象ファイル:
  - `docs/superpowers/plans/2026-05-05-ws-origin-rejection-log-classification.md`
  - `docs/superpowers/specs/2026-05-05-ws-origin-rejection-log-classification-design.md`
  - `docs/todo/TODO.md`
  - `reviews/2026-05-05-branch-81981b2-fb2d45b-review.md`
  - `src/server/guards.rs`
  - `src/server/routes.rs`
- スコープ種別: clean work branch diff from inferred base `origin/develop`

## Agent Dispatch

- `multi-code-reviewer`: correctness / maintainability / KISS / DRY / YAGNI / regression risk
- `multi-security-reviewer`: DNS Rebinding, Host/Origin trust boundary, header log handling
- `multi-test-reviewer`: test adequacy and logging regression coverage
- `multi-comment-reviewer`: comments, docs, TODO accuracy
- `multi-design-reviewer`: helper boundaries and future extensibility
- `multi-observability-reviewer`: audit log level, structured logging, triage usefulness
- `multi-interface-reviewer`: enum/type/API/log contract compatibility
- `multi-ai-output-reviewer`: agent-facing plan/spec safety and prompt-like instruction risk

## Follow-up Fix Status

- Finding 1 fixed: test-only tracing subscriber で実イベント metadata level を捕捉し、Host 系 `ERROR`、`MissingOrigin` `INFO`、`AuthorityMismatch` `WARN` を固定した。
- Finding 2 fixed: plan 冒頭に user approval、branch/worktree、`main`/`develop` 停止、対象外差分確認、commit 実行条件の preflight を追加した。
- Finding 3 fixed: Host 系補足文の制御を表示文字列比較から `host_recheck_anomaly` bool に切り離した。
- Finding 4 fixed: spec/plan のログ出力説明を `emit_ws_rejection_log()` 経由の level dispatch に更新した。
- Finding 5 fixed: final diff scope を branch diff と implementation-only diff の違いが分かる記述に更新した。
- Finding 6 fixed: validation `rg` を禁止文字列に絞り、許容される `WS Origin 拒否` hit を失敗扱いしないようにした。
- Finding 7 fixed: `cargo fmt --all` 実行前後の working tree / changed-file 確認を plan に追加した。
- Finding 8 fixed: WS 拒否ログへ `rejection`, `host`, `origin`, `ws_rejection_class`, `host_recheck_anomaly` の structured fields を追加した。

## Findings

1. [Medium] 実ログの severity dispatch がテストで固定されていない - `src/server/guards.rs:211`
   - Scope relation: Introduced
   - Why: `ws_rejection_log_level()` の戻り値は全 variant で検証されていますが、実際の出力は `emit_ws_rejection_log()` の `match level` で `error!` / `warn!` / `info!` へ再ディスパッチしています。traced log tests は本文だけを確認しており、Host 系拒否が実イベントとして `ERROR` で出ること、`MissingOrigin` が `INFO` で出ること、Origin 系代表が `WARN` で出ることは固定していません。監査ログ分類の主目的が level の段階化なので、dispatch 回帰を検出できないのは観測性の実質的なテストギャップです。
   - Fix hint: テスト用 subscriber で event metadata の level を捕捉し、少なくとも `MissingHost`, `HostMalformed`, `UntrustedHost` が `ERROR`、`MissingOrigin` が `INFO`、代表的な Origin 系拒否が `WARN` で出ることを検証してください。可能なら動的 level を安全に扱える API に寄せて再ディスパッチ自体を減らしてください。
   - Confidence: High

2. [Medium] 実装計画が承認・ブランチ確認なしに commit を指示している - `docs/superpowers/plans/2026-05-05-ws-origin-rejection-log-classification.md:206`
   - Scope relation: Introduced
   - Why: plan は agentic workers 向け手順として `git add` / `git commit` を直接含みますが、同じ文書内で「ユーザー承認済み」「main/develop では停止」「適切な feature/fix branch または worktree 上でのみ実行」といった preflight を再確認していません。AGENTS.md 側にルールはありますが、この plan 単体を実行単位として渡すと、未承認 commit や不適切ブランチへの履歴変更を誘導する余地があります。
   - Fix hint: plan の冒頭に preflight として、ユーザー承認確認、`git status --short --branch` による branch/worktree 確認、`main` / `develop` なら停止、commit は承認済み実装セッションでのみ行う、を明記してください。
   - Confidence: High

3. [Low] ログ分類の制御が表示文字列に依存している - `src/server/guards.rs:191`
   - Scope relation: Introduced
   - Why: `emit_ws_rejection_log()` は `message == "WS Host 検証異常"` で Host 系補足文の有無を決めています。`message` は表示文言なので、将来文言だけを変更したときに Host anomaly context が意図せず外れる可能性があります。通常動作の即時バグではありませんが、Host middleware bypass / malformed probe という security signal の説明が表示文字列に結合しています。
   - Fix hint: 表示文字列ではなく分類値で制御してください。例えば `is_host_middleware_bypass_indicator(rejection)` を `emit_ws_rejection_log()` へ渡す、または `WsRejectionLogClass { level, message, include_host_anomaly_context }` のような小さな分類結果を返す形にします。
   - Confidence: High

4. [Low] ログ出力方式の説明が実装とズレている - `docs/superpowers/specs/2026-05-05-ws-origin-rejection-log-classification-design.md:48`
   - Scope relation: Introduced
   - Why: spec と plan は `tracing::event!` で 1 箇所からログを出すと説明していますが、実装は `emit_ws_rejection_log()` で level ごとに `tracing::error!` / `warn!` / `info!` などへ分岐しています。将来の保守時に、動的 level の `tracing::event!` 前提で読まれる可能性があります。
   - Fix hint: spec/plan を、`emit_ws_rejection_log()` 経由の level-dispatch logging、または `tracing::{error,warn,info}` macro 経由の単一 helper 出力という実装に合わせて更新してください。
   - Confidence: High

5. [Low] 最終差分スコープの期待値が追加ドキュメントを除外している - `docs/superpowers/plans/2026-05-05-ws-origin-rejection-log-classification.md:488`
   - Scope relation: Introduced
   - Why: ブランチ差分には plan/spec 追加も含まれますが、計画書の最終 diff scope 期待値は `src/server/guards.rs`, `src/server/routes.rs`, `docs/todo/TODO.md` のみと書かれています。ブランチ全体の完了確認では false positive になり、逆に docs 追加をレビュー対象から外す誤解も生みます。
   - Fix hint: 期待値に plan/spec 追加を含めるか、「実装コミットのみを確認する場合」など対象範囲を明記してください。
   - Confidence: High

6. [Low] 検証用 `rg` が検索範囲と期待結果の粒度を一致させていない - `docs/superpowers/plans/2026-05-05-ws-origin-rejection-log-classification.md:361`
   - Scope relation: Introduced
   - Why: コマンドは `src/server/guards.rs` 全体から `_ =>` と `WS Origin 拒否` を検索しますが、期待結果は「`is_allowed_ws_origin()` 内に `_ =>` がない」「`WS Origin 拒否` は helper/test のみ」という関数・用途単位です。実際には別関数や test expectation の許容ヒットがあり得るため、エージェントが false positive を失敗扱いして無関係な箇所を変更する誘導になります。
   - Fix hint: `rg -n "_ => warn|WS Host middleware bypass 兆候" ...` のように意図した禁止文字列へ絞るか、`is_allowed_ws_origin()` の範囲だけを確認する手順に分けてください。
   - Confidence: High

7. [Low] `cargo fmt --all` の失敗時手順が対象外差分を巻き込み得る - `docs/superpowers/plans/2026-05-05-ws-origin-rejection-log-classification.md:438`
   - Scope relation: Introduced
   - Why: `cargo fmt --all -- --check` 失敗時に即 `cargo fmt --all` を実行する指示は、作業対象外の Rust ファイルやユーザーの未整理変更まで整形する可能性があります。実装エージェント向け手順として読むと、レビュー範囲外の差分混入につながります。
   - Fix hint: format 実行前に working tree の未コミット差分を確認し、対象外差分がある場合は停止する手順を追加してください。整形後も変更ファイル一覧を確認し、対象外ファイルを含めない条件を明記してください。
   - Confidence: Medium

8. [Low] WS 拒否ログが structured fields ではなく本文に埋め込まれている - `src/server/guards.rs:192`
   - Scope relation: Introduced
   - Why: `emit_ws_rejection_log()` は `rejection`, `host`, `origin`, message class を named field として記録せず、format 済み文字列に埋め込んでいます。運用側で `WsOriginRejection` 別の集計、Host 系 `ERROR` の alert 抑制、Origin 系 `WARN` の切り分けを行う場合、本文 regex に依存し、文言変更で triage が壊れやすくなります。
   - Fix hint: `tracing` の named fields を併用してください。例: `rejection = ?rejection`, `host = ?host`, `origin = ?origin`, `ws_rejection_class = message`, `host_recheck_anomaly = is_host_middleware_bypass_indicator(rejection)`。
   - Confidence: Medium

## Security Notes

- DNS Rebinding 防御の核である Host 信頼判定、Origin authority 信頼判定、Host/Origin authority 一致要求は維持されています。
- Header 値は既存の `log_value_for_header()` 経由で、欠落は `"<absent>"`、非 ASCII は `"<non-ascii>"` に分類されます。
- Host 系 rejection と通常 Origin rejection のログ本文混同は `MissingOrigin` traced test で抑えられています。
- 実イベント level のメタデータと structured fields は follow-up fix でテスト固定済みです。

## Open Questions

- 本番の tracing/log 基盤は named fields を保存・検索できますか。可能なら structured fields 化の価値が高いです。
- `docs/superpowers/plans/*` は常に AGENTS.md を読んだ同一リポジトリ内エージェントだけが実行しますか。単体再利用されるなら、承認/ブランチ preflight を文書内に明示した方が安全です。

## Checks Status

- targeted WS tests: Pass (`cargo test ws_`)
- follow-up structured log test: Pass (`cargo test test_ws_origin拒否実ログは分類levelと構造化fieldを出力する`)
- format: Pass (`./verify.sh` 内で確認済み)
- lint: Pass (`./verify.sh` 内 clippy 確認済み)
- tests: Pass (`cargo test --all-targets --all-features`, `./verify.sh` の debug/release tests)
- CI: Unknown

## Per-Agent Results

- Code: Findings なし。Targeted tests / clippy / fmt を一部再実行して pass。
- Security: Findings なし。ログ保管基盤側の長大値制限は未確認。
- Test: Medium 1 件。実イベント level の未固定を指摘。
- Comments/Docs: Low 3 件。spec/plan の実装ズレ、最終差分スコープ、commit 手順 preflight を指摘。
- Design: Low 1 件。表示文字列依存の分類制御を指摘。
- Observability: Low 1 件。structured fields 不足を指摘。
- Interface: Findings なし。WebSocket 拒否時の JSON 応答と enum/type contract は維持。
- AI-output / prompt-like docs: Medium 1 件、Low 2 件。agent 向け plan の git 操作 preflight と検証手順の粒度を指摘。

## Skipped Perspectives

- Config: CI、依存関係、Docker、環境変数、build setting、lockfile 変更がないため未 dispatch。
- Silent failure: 主変更はログ分類とテストで、エラー握りつぶし、nullable fallback、継続失敗経路の変更ではないため未 dispatch。

## Residual Risk

- Follow-up fix 後の full verification は pass 済みです。
- 実ブラウザ WebSocket 接続、実運用 tracing subscriber、ログ保存基盤の field support / 長さ制限 / エスケープ挙動は未確認です。
- `OriginMissingAuthority` は現行 `http` crate で直接到達入力がない defensive variant のままです。
