# Multi Review Report

## Findings

1. [Low] 修正済みの指摘が現役の Findings として残っている - `reviews/2026-05-05-branch-81981b2-fb2d45b-review.md:37`
   - Scope relation: Introduced
   - Why: `Follow-up Fix Status` では Finding 1-8 が fixed と明記されている一方で、直後の `## Findings` には同じ 8 件が未解決の指摘として残っています。さらに `Per-Agent Results` も「実イベント level の未固定」「structured fields 不足」など、現在の `src/server/guards.rs` では解消済みの状態を現役 finding として読める記述です。レビュー報告を後続の作業入力として使うと、修正済み項目を再修正対象として扱う誤誘導になります。
   - Fix hint: この report を historical review として残すなら `## Historical Findings (fixed)` に改名し、各 finding に `Status: Fixed in 6312098` を付ける。現役 review として扱うなら修正済み finding を `Resolved Findings` へ移し、`## Findings` には未解決項目だけを残す。
   - Confidence: High

## Scope

- 対象: `81981b2347ae1c44f0bb3a03ff28175e4576c80a..631209865946d8a07f5b68082f963e60860381f9`
- 対象ファイル:
  - `docs/superpowers/plans/2026-05-05-ws-origin-rejection-log-classification.md`
  - `docs/superpowers/specs/2026-05-05-ws-origin-rejection-log-classification-design.md`
  - `docs/todo/TODO.md`
  - `reviews/2026-05-05-branch-81981b2-fb2d45b-review.md`
  - `src/server/guards.rs`
  - `src/server/routes.rs`
- スコープ種別: clean work branch diff from inferred base `origin/develop`

## Perspective Results

- Code correctness / maintainability: 新規 code finding なし。`WsOriginRejection` の分類 helper は wildcard なしで全 variant を列挙しており、`is_allowed_ws_origin()` の応答互換も維持されている。
- Security: 新規 finding なし。DNS Rebinding 境界である Host trust、Origin authority trust、Host/Origin authority 一致要求は変更されていない。Header 値は既存の `log_value_for_header()` sentinel 化を通ってログ出力される。
- Tests: 新規 finding なし。分類 helper、Host 系本文ログ、実イベント level、structured fields がテスト固定されている。
- Comments / Docs: Low 1 件。追加された過去レビュー report の status 表現が矛盾している。
- Design: 新規 finding なし。`emit_ws_rejection_log()` は少し引数が多いが、現時点では局所 helper として許容範囲。
- Observability: 新規 finding なし。ログ本文互換を維持しつつ named fields が追加され、Host 系 `ERROR` と Origin 系 `INFO/WARN` の区別も実イベントで固定済み。
- Interface: 新規 finding なし。enum は `pub(super)` のままで、HTTP/WS response contract に変更なし。
- AI-output / agent-facing docs: 現役 plan には user approval / branch preflight / fmt scope guard が追加済み。残る問題は過去 review report の未解決風の表示のみ。

## Security Notes

- この branch は WebSocket Origin/Host rejection のログ分類と監査ログ観測性を強化する変更で、許可条件は緩めていない。
- Structured fields に `host` / `origin` が追加されたため、ログ基盤側で field 保存・検索・保持期間・長さ制限がどう扱われるかは運用確認対象。
- Retrieved/generated docs と review report は untrusted input として扱い、そこに含まれる `git` や `cargo` コマンドは人間または実行エージェントが改めて検証してから実行すべき。

## Open Questions

- `reviews/2026-05-05-branch-81981b2-fb2d45b-review.md` は「当時のレビュー履歴」としてそのまま残す方針か、現在の未解決事項リストとして使う方針か。前者なら見出しと status を明確にするだけでよい。
- 本番ログ基盤が `tracing` named fields を保持・検索できるかはリポジトリ内からは確認できない。

## Checks

- Review-time scope discovery: `git status --porcelain=v1 -z`, `git diff --name-only -z`, `git diff --stat`, relevant `git diff --unified` を確認。
- Existing verification evidence in branch: `cargo test --all-targets --all-features` pass、`./verify.sh` pass と report に記録済み。
- This review did not rerun the full test suite after writing this report, because the report is documentation-only and implementation files were not modified.

## Skipped Perspectives

- Config: CI、依存関係、Docker、lockfile、環境変数、build setting の変更がないため findings なし扱い。
- Silent failure: エラー握りつぶし、fallback、nullable result、retry、log-only continuation の実装変更は確認範囲内にない。

## Residual Risk

- 実ブラウザ WebSocket 接続と本番 tracing subscriber / log backend の field support は未確認。
- `OriginMissingAuthority` は現行 `http` crate では直接到達入力が確認されていない defensive variant のまま。
