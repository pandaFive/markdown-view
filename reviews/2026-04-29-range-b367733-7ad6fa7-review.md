# Multi Review Report

## Scope

- 対象: `develop..docs/render-markdown-responsibility-split`
- Base: `b36773369e065214aebc014cf12a4869eada962a`
- Head: `7ad6fa7376664821ed264eac3100a1559c4f2c9f`
- 変更量: 10 files changed, 2396 insertions(+), 663 deletions(-)
- 対象ファイル:
  - `docs/superpowers/plans/2026-04-29-render-markdown-responsibility-split.md`
  - `docs/superpowers/specs/2026-04-29-render-markdown-responsibility-split-design.md`
  - `docs/todo/BACKLOG.md`
  - `src/renderer/highlight.rs`
  - `src/renderer/line.rs`
  - `src/renderer/mod.rs`
  - `src/renderer/render.rs`
  - `src/renderer/security.rs`
  - `src/renderer/state.rs`
  - `tests/renderer_test.rs`

## Agent Dispatch

- `multi-code-reviewer`: correctness, simplicity, maintainability, KISS/DRY/YAGNI/PIE, regression risk
- `multi-security-reviewer`: XSS, escaping, URL sanitization, untrusted Markdown handling, unsafe boundary
- `multi-test-reviewer`: test coverage, boundary cases, regression tests
- `multi-interface-reviewer`: public/internal API, type boundaries, module contracts
- `multi-ai-output-reviewer`: prompt/agentic docs, AI workflow artifacts, unsafe instruction reuse
- `multi-comment-reviewer`: comments, docs, plans, stale or misleading explanations
- `multi-silent-failure-reviewer`: swallowed errors, default fallbacks, nullable/hidden failure modes
- `multi-observability-reviewer`: logs, diagnostics, operational debugging support

## Findings

### Medium

1. `docs/superpowers/plans/2026-04-29-render-markdown-responsibility-split.md:3`  
   完了済み作業の plan が、未実行の agentic checklist として残っています。先頭に `REQUIRED SUB-SKILL` があり、本文にも未チェックの `- [ ]` と実行用 git command が残るため、将来のエージェントが完了済み作業を再実行するリスクがあります。  
   推奨対応: plan を「実行済み履歴」と明示し、再実行禁止または履歴用途であることを冒頭に追記する。必要なら checklist を完了状態または archive 形式へ更新する。

### Low

2. `src/renderer/state.rs:98`  
   `push_heading_safe_html(&str)` が安全な HTML 断片であることを型ではなく命名だけで表しています。現在の呼び出し元は escape 済み断片に限定されており即時の脆弱性ではありませんが、将来の変更で未エスケープ文字列を渡すと `SanitizedHtml` 境界の前提が崩れます。  
   推奨対応: `TrustedHtmlFragment` / `EscapedHtmlFragment` のような newtype を導入するか、少なくとも「rendered fragment」系の命名にして安全性を型で主張しない。

3. `docs/superpowers/plans/2026-04-29-render-markdown-responsibility-split.md:57`  
   plan 内の境界テスト期待値が実テストと不一致です。plan snippet は inline code の source line を `6` としていますが、実装済みの `tests/renderer_test.rs:525` は `7` を期待しています。  
   推奨対応: plan の期待値を `data-source-start-line="7" data-source-end-line="7"` に修正する。

4. `docs/superpowers/specs/2026-04-29-render-markdown-responsibility-split-design.md:46`  
   RenderState の責務説明が実装より狭く書かれています。spec は RenderState が URL sanitize や syntax highlight の判断を持たないと説明していますが、実装では `state.rs` が security/highlight helper を呼び出して安全な断片を組み立てています。  
   推奨対応: 「policy/options は Renderer、RenderState は helper を呼び出して安全な断片を蓄積する」趣旨に更新する。

5. `docs/todo/BACKLOG.md:63`  
   Done 根拠として `cargo test --all-targets --all-features` と `./verify.sh` が固定されていますが、実行日時、commit、CI link、ログ参照がありません。後続作業者が現在も検証済みと誤認する可能性があります。  
   推奨対応: 実行日、対象 commit、実行者メモ、CI run など追跡可能な証跡を追加するか、「実装時点で pass と報告」と表現を弱める。

### Info

6. `src/renderer/render.rs:12`  
   `RenderOptions` が拡張口に見えますが、`pub(super)` かつ field private のため親 module から実質的に `Default` 以外を設定できません。現時点では問題ではありません。  
   推奨対応: しばらく設定差分が不要なら `Renderer::new(input)` に畳む。将来の拡張前提なら constructor/builder を追加する。

7. `docs/todo/BACKLOG.md:61` / `docs/superpowers/plans/2026-04-29-render-markdown-responsibility-split.md`  
   完了ファイル一覧に `src/renderer/toc.rs` が含まれていますが、この range では変更されていません。変更ファイル一覧として読むと誤解を招きます。  
   推奨対応: `toc.rs` を削除するか、「確認対象」として分類を分ける。

## Security Notes

- untrusted Markdown 由来の HTML escape、URL sanitize、raw HTML escaping、source line metadata の基本経路に重大な指摘はありません。
- `SanitizedHtml` 境界そのものを弱める変更は見つかりませんでした。
- 残る主な security-adjacent risk は `push_heading_safe_html(&str)` の内部 API 境界です。現状は安全な呼び出し元に依存しており、将来変更で誤用される余地があります。
- retrieved text や AI 生成 plan を実行指示として再利用しないため、完了済み plan の archive 表現を明確にする必要があります。

## Open Questions

- `html_escape`, `slugify`, `generate_unique_id`, `extract_headings` は今後も stable external API として扱う想定ですか。
- `docs/superpowers/plans/` は active な実行計画置き場ですか、それとも完了後は historical artifact として扱う運用ですか。
- `./verify.sh` の実行ログまたは CI run への参照を Done 記録に残す運用にしますか。

## Checks Status

- `cargo test --all-targets --all-features`: Pass (`multi-observability-reviewer` が実行)
- `cargo fmt --all -- --check`: この review では未実行
- `cargo clippy --all-targets --all-features -- -D warnings`: この review では未実行
- `./verify.sh`: この review では未実行
- CI: 未確認

参考: 実装完了時点では `cargo fmt`, `cargo clippy`, `cargo test`, `./verify.sh` が pass と報告されています。ただし、この multi-review では full verify を再実行していません。

## Per-Agent Results

- `multi-code-reviewer`: Findings none. Renderer 分割の責務境界、KISS/DRY/YAGNI/PIE、regression risk を確認。
- `multi-security-reviewer`: Findings none. XSS、escaping、URL sanitize、untrusted Markdown、source line metadata、logging を確認。
- `multi-test-reviewer`: Findings none. 境界テスト、source line tracking、HTML sanitize regression の coverage を確認。
- `multi-silent-failure-reviewer`: Findings none. fallback/default handling、unsupported event debug、syntax highlight fallback、URL sanitize fallback を確認。
- `multi-observability-reviewer`: Findings none. Logging/diagnostic impact を確認し、`cargo test --all-targets --all-features` を実行して pass。
- `multi-interface-reviewer`: Low 1件、Info 1件。内部 API の safety contract と `RenderOptions` 拡張口を指摘。
- `multi-ai-output-reviewer`: Medium 1件、Low 1件。完了済み plan の再実行リスクと検証証跡不足を指摘。
- `multi-comment-reviewer`: Low 2件、Info 1件。plan/spec/BACKLOG の stale documentation を指摘。

## Skipped Perspectives

- `multi-config-reviewer`: CI、dependency、Docker、lockfile、runtime configuration の変更が対象 range に含まれないため skip。

## Residual Risk

- Full verify はこの review では再実行していないため、format/lint/統合検証の最新状態は未確認です。
- Browser/E2E 表示確認は未実行です。ただし対象は renderer core と docs が中心で、UI asset や frontend runtime の変更はありません。
- ドキュメント artifact が active plan と historical record のどちらとして扱われるかが曖昧なため、運用上の再実行リスクが残っています。
