# 完了タスク (2026-04-19)

このファイルには 2026-04-19 に完了したタスクが記録されています。

---

## バッチリファクタ（低優先度クリーンアップ）

- [x] `handle_socket` 内の `if let Some` + `match` のネストを 2 ステップに分離
  - 完了時刻: 13:30
  - ブランチ: `refactor/batch-low-priority-cleanup`
  - PR: 後述（バッチPR）
  - 変更内容:
    - `src/server/session.rs:24-51`: `if let Some(update) = match {...} {...}` の二重ネストを「中間変数 `initial` に Result→Option を束縛 → `if let Some(update) = initial` で分岐」に分離
  - 理由: エラーハンドリングと Option 分岐が視覚的に重なって読みづらかった。中間束縛により Result 処理と Option 分岐が順序立てて読めるようになる
  - TODO 記載の修正: 旧 TODO の対象ファイル `src/server/websocket.rs` は `session.rs` / `broadcast.rs` に分割済みのため、正しいパスで対応

- [x] `build_resolved_target` のコメント詳細化
  - 完了時刻: 13:32
  - ブランチ: `refactor/batch-low-priority-cleanup`
  - PR: 後述（バッチPR）
  - 変更内容:
    - `src/server/files/resolve.rs:261`: 1 行の graceful degradation コメントを 5 行に拡張。HTTP 経路（描画継続）と WebSocket 変更通知経路（`build_update_target` 側でブロードキャスト抑止）の責務分離を明示
  - 理由: 旧 5 行コメントが簡略化された際に、WebSocket 経路で不整合な更新を出さないための抑止ロジックへの参照が失われ、保守者向け情報が減少していた

- [x] `MemoResponse::new` を `from_raw` に置換し raw/html 整合を内部化
  - 完了時刻: 13:35
  - ブランチ: `refactor/batch-low-priority-cleanup`
  - PR: 後述（バッチPR）
  - 変更内容:
    - `src/template/message.rs`: `pub fn new(raw, html, file)` を削除し `pub fn from_raw(raw, file)` を新設。html は内部で `render_markdown(&raw)` により生成
    - `src/template/message.rs`: `empty(file)` も `from_raw` 経由に簡約
    - `src/server/files/memo.rs`: 3 つの呼び出しサイト（GET/PUT/legacy fallback）を `from_raw` に置換、`render_markdown` の重複呼び出しと `raw.clone()` を削除
    - `src/server/files/memo.rs`: 不要になった `use crate::renderer::render_markdown` を削除
    - `src/template/mod.rs`: テスト 3 箇所を `from_raw` / `empty` に更新
  - セキュリティ観点: raw → html の変換経路を 1 箇所に集約することで、XSS サニタイズの一貫性を強化
  - API 互換性: `MemoResponse` は pub だがクレートはバイナリ専用（`Cargo.toml` に `[lib]` 記載なし、未公開）。HTTP API のレスポンス JSON shape (raw/html/file) は不変。Rust API のみの内部互換性破壊を CLAUDE.md「外部公開 API の互換性破壊は明示的に扱え」に従い、コミットメッセージ・PR 本文で明示

- [x] ⛔ 取下げ: `render_markdown("")` の OnceLock キャッシュ
  - 完了時刻: 13:00（事前調査で判定）
  - 理由: `src/renderer/mod.rs:184-187` で `if input.is_empty() { return SanitizedHtml::from_sanitized_html(String::new()); }` の早期リターンが既に存在。キャッシュ可能なコストは `String::new()` 1 回分のみ。`SanitizedHtml` 取得時の Clone も同等コストのため、ネット効果ゼロ
  - 判断: 計測値なしの推測上のマイクロ最適化を入れることでコード複雑度だけ増えるため、TODO ごと取下げ

## メモ機能 参照導線改善エピック

- [x] レンダラー出力に行範囲ジャンプ用の安定ターゲット（data-line-block）を追加
  - 完了時刻: 15:00
  - ブランチ: `feat/memo-source-jump-epic`
  - PR: 後述（エピックPR）
  - 変更内容:
    - `src/renderer/mod.rs`: block_line_attrs() ヘルパー新設。<p>/<h*>/<pre>/<blockquote>/<ul>/<ol>/<li>/<table> に `data-line-block` 属性を付与
    - 既存 `data-source-start-line/end-line` は span/inline 用に維持
    - `tests/renderer_test.rs`: block要素への付与・インライン要素への非付与を検証するテスト7件追加
  - 理由: JS側が `[data-line-block]` セレクタで行範囲ジャンプの候補を限定できるよう、ターゲット識別属性を整備

- [x] メモプレビュー内の出典クリックで本文の対応ブロックへスクロール
  - 完了時刻: 15:15
  - ブランチ: `feat/memo-source-jump-epic`
  - PR: 後述（エピックPR）
  - 変更内容:
    - `src/template/assets/js/memo.js`: buildQuoteSource で href 末尾に `:L5-L7` フラグメントを追加、リンクラベルに `(L5-L7)` を埋め込み
    - `src/template/assets/js/content.js`: parseLineHash で `:L5-L7` または単独 `#L5-L7` を検出、scrollToLineRange で `[data-line-block]` を線形検索して対応ブロックへ smooth scroll
  - 理由: 行番号がリンク内に含まれることでクリック時に行範囲を受け取れるようになり、heading 単位より細かいジャンプを実現

- [x] 本文ジャンプ時の一時ハイライト表示を追加
  - 完了時刻: 15:20
  - ブランチ: `feat/memo-source-jump-epic`
  - PR: 後述（エピックPR）
  - 変更内容:
    - `src/template/assets/js/content.js`: triggerJumpHighlight 関数。`.jump-highlight` クラス付与 + animationend で除去（force reflow でアニメ再生）
    - `src/template/assets/css/content.css`: @keyframes jump-highlight-fade。黄色背景 rgba(250,204,21,0.45) が 2.5秒で fade out
  - 理由: 長文ドキュメントでスクロールだけだと着地点が視認しづらい問題を解消。既存 .document-search-match と同系統の色で一貫性維持

- [x] 出典ジャンプ導線の E2E テストを追加
  - 完了時刻: 15:30
  - ブランチ: `feat/memo-source-jump-epic`
  - PR: 後述（エピックPR）
  - 変更内容:
    - `tests/e2e/memo_jump.spec.js` (新規): 本文選択→引用追加→出典リンククリック→スクロール位置変化と `.jump-highlight` クラスの一時付与・除去を検証
    - `tests/fixtures/e2e/long.md` (新規): 4セクション×3段落の長文 fixture。TARGET BLOCK を中盤に配置
    - `tests/e2e/memo_quote.spec.js`: 出典フォーマット変更（`[label](href) L3` → `[label (L3)](href:L3)`）に伴う assertion 更新
  - 理由: renderer 属性変更、JS 動作、CSS アニメーション、メモ出典フォーマット変更の相互作用を統合検証

## テスト追加

- [x] `MemoResponse::from_raw` の契約テストを追加
  - 完了時刻: 14:20
  - ブランチ: `test/memo-response-contract-and-memo-ui`
  - PR: 後述（バッチPR）
  - 変更内容:
    - `src/template/message.rs`: `#[cfg(test)] mod tests` を新設し4件の単体テストを追加
      - `test_from_raw_htmlがrender_markdownと一致する`: 空文字列・見出し・リスト・引用の4入力で `from_raw(s, _).html()` が `render_markdown(s)` と完全一致
      - `test_from_raw_rawフィールドは入力を改変せず保持する`: `raw()` が入力文字列を改変せず返す
      - `test_from_raw_fileフィールドが保持される`: `file()` の Some/None
      - `test_empty_は空文字列をfrom_rawしたものと等価`: `empty(file)` と `from_raw(String::new(), file)` の raw/html/file が一致
  - 理由: PR #71 で導入した新契約「html ≡ render_markdown(raw)」を直接検証する単体テストを整備し、将来の内部実装変更（pre-trim、サニタイズ追加等）に対する回帰検出を可能にする
  - 出典: PR #71 pr-test-analyzer レビューの Suggestion（criticality 3/5）

- [x] `test_メモuiが描画される` にメモ本文の挙動アサーションを復活
  - 完了時刻: 14:25
  - ブランチ: `test/memo-response-contract-and-memo-ui`
  - PR: 後述（バッチPR）
  - 変更内容:
    - `src/template/mod.rs:655付近`: `assert!(html.contains("<blockquote"), ...)` を追加
  - 理由: PR #71 で raw/html 統一時にメモ本文の描画経路を検証するアサーションが消滅していた。blockquote 記号 `>` から `<blockquote>` 要素が出ることで、raw → render_markdown → render_page の描画パイプライン全体を回帰検出可能にする
  - 出典: PR #71 pr-test-analyzer レビューの Suggestion（criticality 2/5）

- [x] `build_lagged_recovery_message` の `ReadFailed` 分岐を直接テスト
  - 完了時刻: 12:30
  - ブランチ: `test/lagged-recovery-readfailed-coverage`
  - PR: #70
  - 変更内容:
    - `src/server/files/tests.rs`: サイズ超過ファイル（`MAX_FILE_SIZE + 1`）に対して `BroadcastMessage::Error` が「ファイル読み込みエラー」プレフィックスとサイズ上限メッセージを含むことを検証するテストを追加
    - `src/server/files/tests.rs`: 非UTF-8 バイト列（`0xff,0xfe,0xfd`）の `.md` ファイルに対して `BroadcastMessage::Error` が「ファイル読み込みエラー」プレフィックスとUTF-8エラーメッセージを含むことを検証するテストを追加
  - 理由: 既存テストは `ResolveFailed` 分岐（ファイル削除→NotFound）のみカバーしており、`ReadFailed` 分岐は `load_route_update` 経由で間接的に担保されていた。共通経路 `validate_and_render` が将来分岐した際のリグレッション検出を強化するため、`build_lagged_recovery_message` 側から直接カバレッジを追加
  - 出典: PR #69 pr-test-analyzer レビューの Suggestion（criticality 4-5）

## 軽微なリファクタ・保守改善

- [x] `lagged_recovery_message` 委譲関数を除去
  - 完了時刻: 01:00
  - ブランチ: `refactor/remove-lagged-recovery-delegation`
  - PR: #69
  - 変更内容:
    - `src/server/broadcast.rs`: 委譲関数 `lagged_recovery_message`（`build_lagged_recovery_message` への純粋委譲）を削除
    - `src/server/broadcast.rs`: `files/tests.rs` と重複する単一ファイル/ディレクトリモードのテスト 2 件を削除
    - `src/server/session.rs`: `build_lagged_recovery_message` を直接呼び出すように import と call site を変更
    - `src/server/files/tests.rs`: 失敗パステスト（ファイル削除後に `Error` が返る）を移植追加
  - 理由: 呼び出し経路の中間層を除去して読解コストを低減。重複テストを削減しつつ、ユニークな失敗パスカバレッジは files/tests.rs に保持
  - TODO 記載の修正: 旧 TODO の対象ファイル `src/server/websocket.rs` は `session.rs` / `broadcast.rs` に分割済みのため、正しいパスで対応

- [x] `file_label` 算出ロジックの重複を `file_display_name` 共通関数に抽出
  - 完了時刻: 00:03
  - ブランチ: `refactor/extract-file-display-name`
  - PR: #68
  - 変更内容:
    - `src/server/files/resolve.rs` に `pub(super) fn file_display_name(path: &Path) -> String` を追加（file_name → display フォールバック）
    - `ResolvedTarget::new` の fallback 連鎖を `file_display_name` 呼び出しに簡素化
    - `src/server/files/content.rs` のローカル関数 `display_name` を削除、呼び出し元を共通関数に差し替え
  - 理由: `ResolvedTarget::new` と `display_name` で同じ file_name → display ロジックが重複していたため、pub(super) 関数として抽出し DRY を達成。将来の表示ラベル方針変更を 1 箇所で行えるように集約
  - TODO 記載の修正: 旧 TODO の「`update_broadcast_message`」は PR #58 で `build_change_broadcast_message` に改名済みのため、新しい関数名で対応
