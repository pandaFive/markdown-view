# 完了タスク (2026-04-20)

## augmentHashWithTrailingLineHint 分岐カバレッジ補強

- [x] `augmentHashWithTrailingLineHint` 残りの分岐テスト追加
  - 完了時刻: 実装完了時点
  - ブランチ: `test/augment-hash-branch-coverage`
  - PR: #76
  - 変更内容:
    - `tests/e2e/memo_jump.spec.js`: 未カバーだった 2 分岐を unit-like E2E テストで補強
      1. precedence 早期 return: `hash === '#section-b:L15'` のとき sibling `L20` で上書きされず元の hash が返る (`content.js:188`)
      2. 空 hash 合成経路: `hash === ''` / `hash === '#'` のとき `'#L<n>'` が生成される（`'#:L<n>'` にならない） (`content.js:197`)
  - 理由: PR #75 レビューで指摘された criticality 3 testing gap の残作業。本体機能は full-flow テストでカバー済みだが、分岐レベルの明示検証が欠けていた
  - 影響範囲: テストコードのみ（プロダクションコード変更なし）

## 目次クリック直後の逆方向スクロールで通常判定へ戻す

- [x] `text_selection_defer.spec.js:333` 目次クリック直後の逆方向スクロール判定が誤動作
  - ブランチ: `fix/toc-reverse-scroll-grace`
  - PR: #77
  - 変更内容:
    - `src/template/assets/js/sidebar.js`: popstate handler に pending TOC navigation と一致する hash の再処理スキップを追加（9行）
    - `tests/e2e/text_selection_defer.spec.js`: origin 付近の小揺らしで pending が保持される保証を 1 件追加
  - 根本原因: 目次クリック直後にブラウザが同一document hash navigation で発火させる popstate が `restoreContentNavigationFromLocation → scrollIntoView` を走らせ、ユーザの明示的 `window.scrollTo` を上書きしていた
  - 影響範囲: ディレクトリモードの popstate 経路（同一ファイル hash のみ）、目次クリック直後の active 判定挙動

## updateContent の no-op 判定キャッシュ化

- [x] enhanceContentInteractions の DOM 改変で常に mismatch していた構造的欠陥を修正
  - 完了時刻: 作業完了時
  - ブランチ: `fix/update-content-noop-cache`
  - PR: 後続 Task 6 で作成
  - 変更内容:
    - `src/template/assets/js/bootstrap.js`: `lastAppliedContent` global var を追加 (null 初期化)
    - `src/template/assets/js/content.js`: `updateContent` L1239 の比較対象を contentEl の現在 HTML から `lastAppliedContent` に変更し、描画時に cache 更新。末尾に `window.updateContent` テスト用 expose
    - `tests/e2e/memo_jump.spec.js`: `waitForTimeout(1000)` 2 箇所と関連 race 解説コメントを削除。「同じ data.content での 2 回目 updateContent はジャンプハイライトを消さない」回帰テストを追加 (1 回目 prime → ハイライト付与 → 2 回目 no-op 検証の 3 段フロー)
    - `docs/superpowers/specs/2026-04-20-update-content-noop-fix-design.md`: 設計書
    - `docs/superpowers/plans/2026-04-20-update-content-noop-fix.md`: 実装プラン
  - 設計変更の経緯:
    - 当初 A1 設計 (`lastAppliedContent = contentRoot.innerHTML` で SSR HTML をキャッシュ) を採用したが、ブラウザ HTML 正規化 (attribute 順序・quote 種・whitespace 等) で SSR HTML と WS 由来 `data.content` が完全一致しないため初回 broadcast で必ず再描画される欠陥が判明
    - A2 設計 (`lastAppliedContent = null` 初期化、初回 broadcast での再描画を許容) に切り替え。初回再描画は UI 上 `.jump-highlight` 付与前タイミングのため実害なし
    - 回帰テストも A2 に合わせ「prime + 2 回目」フローに改修
  - 理由: PR #79 で test 側に入れていた `waitForTimeout(1000)` 1000ms workaround を product 側で根治。UI 上の「リンククリック直後のハイライト一瞬消失」軽微 visual バグも同時解消。将来同様の一時状態 (アニメーション/ハイライト) を追加した際の再発防止
  - テスト結果: 全 E2E spec 79 件 PASS、`memo_jump.spec.js` の所要時間が 25.9s → 4.9s と約 21 秒短縮、`./verify.sh` 全 PASS
