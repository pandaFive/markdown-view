# 完了タスク (2026-04-18)

このファイルには 2026-04-18 に完了したタスクが記録されています。

---

## watcher API リファクタリング

- [x] `WatchThreadConfig`の`&'static str`フィールドを`WatchStrategy`メソッドに統合
  - 完了時刻: 作業完了時
  - ブランチ: `refactor/watcher-strategy-labels`
  - PR: #57
  - 変更内容:
    - `WatchThreadConfig` 構造体と `WatchStrategy::runtime_config()` を削除
    - `WatchStrategy` に 9 メソッド（`watch_dir`/`recursive_mode`/`thread_name`/`unexpected_exit_message`/`start_error_prefix`/`watch_error_prefix`/`panic_message`/`change_label`/`error_label`）を追加
    - `spawn_watcher_thread` のシグネチャを `(WatchStrategy, PathBuf, ...)` に簡素化
    - 旧 `test_runtime_config_*` テスト 2 件を新メソッド検証用の `test_strategy_*` に差し替え
  - 理由: 静的ラベル群を保持するだけの中間構造体を排除し、モード依存定数の単一ソースを `WatchStrategy` に統合。match 駆動で新モード追加時の漏れをコンパイルエラーで検出可能にした
