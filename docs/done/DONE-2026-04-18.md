# 完了タスク (2026-04-18)

このファイルには 2026-04-18 に完了したタスクが記録されています。

---

## watcher API リファクタリング

- [x] `WatchThreadConfig`の`&'static str`フィールドを`WatchStrategy`メソッドに統合
  - 完了時刻: 作業完了時
  - ブランチ: `refactor/watcher-strategy-labels`
  - PR: #57
  - 変更内容:
    - `WatchThreadConfig` 構造体と `WatchStrategy::runtime_config()` を削除
    - `WatchStrategy` に 9 メソッド（`watch_dir`/`recursive_mode`/`thread_name`/`unexpected_exit_message`/`start_error_prefix`/`watch_error_prefix`/`panic_message`/`change_label`/`error_label`）を追加
    - `spawn_watcher_thread` のシグネチャを `(WatchStrategy, PathBuf, ...)` に簡素化
    - 旧 `test_runtime_config_*` テスト 2 件を新メソッド検証用の `test_strategy_*` に差し替え
  - 理由: 静的ラベル群を保持するだけの中間構造体を排除し、モード依存定数の単一ソースを `WatchStrategy` に統合。match 駆動で新モード追加時の漏れをコンパイルエラーで検出可能にした

## server files 取得フロー集約

- [x] validate-build-render パターンの3重重複を共通ヘルパーに抽出
  - 完了時刻: 作業完了時
  - ブランチ: `refactor/content-validate-render-helper`
  - PR: #58
  - 変更内容:
    - `ValidateRenderOutcome` enum（Empty / Resolved / ResolveFailed / ReadFailed）を追加
    - 共通ヘルパー `validate_and_render` を追加し、resolve 結果を受けて read/render を実行
    - `load_initial_socket_update`, `build_lagged_recovery_message`, `build_change_broadcast_message` を outcome match に書き換え
  - 理由: 3関数の resolve → read/render → 結果構築の骨格が共通していたため、中間型で制御フローを統一。成功型・エラー型・ログラベルは各関数固有として残し、ラッパー側に関心を集約した

## テスト追加

- [x] `load_initial_socket_update` のエラーパステスト追加
  - 完了時刻: 作業完了時
  - ブランチ: `test/initial-socket-update-error-paths`
  - PR: #60
  - 変更内容:
    - `test_load_initial_socket_update_ディレクトリモードではnoneを返す`（`NoTarget` arm）を追加
    - `test_load_initial_socket_update_単一ファイル削除時は1008エラーを返す`（`ResolveFailed → close_code 1008`）を追加
    - `test_load_initial_socket_update_単一ファイルサイズ超過時は1009エラーを返す`（`ReadFailed::TooLarge → close_code 1009`）を追加
  - 理由: outcome → `SocketInitError::close_code` のワイヤリングを固定し、将来 arm を取り違えた変更をリグレッション検出可能にする

## TODO.md stale 項目の解消（既存実装で達成済み）

- [x] `handle_debounced_events()` のユニットテスト追加（stale: リファクタ後に既存カバレッジが存在）
  - ブランチ: `docs/cleanup-stale-todos`
  - 達成箇所:
    - 当時の `src/watcher.rs` はモジュール分割され、`handle_debounced_events()` は `WatchStrategy::collect_changed_paths()` に置き換わった
    - TODO が要求していた 5 テスト（SingleFile / Directory 非md / 隠しパス / 重複排除 / ベース外）は `src/watcher/strategy.rs:352-454` に存在
  - 理由: ファイル構造・関数名の変更で TODO 記載が古くなったため、現状に合わせて stale クローズ

- [x] `Watcher::spawn` ディレクトリモードの end-to-end テスト追加（stale: 既存カバレッジ）
  - ブランチ: `docs/cleanup-stale-todos`
  - 達成箇所: `src/watcher/runtime.rs:328` に `test_watcher_spawn_ディレクトリモードでイベント受信できる` が存在
  - 理由: リファクタに伴い該当テストが既に存在していた

- [x] `WatchStrategy` で `CanonicalPath` 型を使用（stale: 既存実装）
  - ブランチ: `docs/cleanup-stale-todos`
  - 達成箇所: `src/watcher/strategy.rs:12-13` で `SingleFile { target_path: CanonicalPath }` / `Directory { base_dir: CanonicalPath }` として既に採用済み
  - 理由: ウォッチャーモジュール再編時に型安全化が適用済み

## 型・可視性整理

- [x] `CanonicalPath` の可視性を縮小
  - ブランチ: `refactor/narrow-canonical-path-visibility`
  - PR: #62
  - 変更内容: `CanonicalPath` を `pub` → `pub(crate)` へ降格、`src/server.rs:16-17` の再エクスポートを `pub(crate) use` / `pub use` に分離
  - 軌道修正: TODO 原文の `pub(super)` は `src/watcher/strategy.rs` がクレート直下の兄弟モジュールから参照するためコンパイル不能。実現可能な最大縮小として `pub(crate)` を採用
  - スコープ外: `CanonicalPathError` は `AppModeBuildError::CanonicalPath(CanonicalPathError)` variant 経由で公開 API を構成するため縮小不可、`pub` 維持
  - 理由: lib+bin 構成で外部クレートから `markdown_view::server::CanonicalPath` が到達可能だったため API 面を縮小

- [x] `SocketInitError` の可視性整理（stale: TODO 前提が崩れているため won't fix）
  - ブランチ: `docs/close-stale-visibility-todos`
  - 分析結果: TODO は「`pub(super)` 縮小 or 再エクスポート追加」の 2 択を提示したが、両方とも実益がない:
    - **`pub(super)` 縮小は不可能**: `src/server/session.rs:30` が `error.close_code()` / `error.reason()` をメソッド呼び出しで使用しており、files モジュール外からのメソッド可視性が必要。現状の `pub(in crate::server)` は過剰宣言ではなく必要最小
    - **再エクスポート追加は cosmetic-only**: `SocketInitError` を名前でインポートしている呼び出し元はゼロ（session.rs・tests.rs 共にメソッド呼び出しのみ）。追加しても直接の機能利得なし
  - 結論: TODO の前提（「実質 `pub(super)` 相当で縮小余地あり」）が事実に反するため、won't fix として解消
  - 理由: 可視性設計は現状で正しく、変更の必要性がない

## メモ機能: リアルタイム同期

- [x] Step 1. メモ更新用の WebSocket メッセージ仕様を追加
  - 完了時刻: 20:40
  - ブランチ: `docs/move-memo-sync-tasks`（記録のみ、実装は PR #54 関連）
  - PR: #54（"fix: メモ同期更新の取りこぼしを防止"）関連コミットで統合的に実装
  - 変更内容: `src/server/messages.rs` に `MemoUpdate` 型相当のメッセージを追加、本文更新メッセージと別 type で対象ファイル識別子を必須化
  - 理由: リアルタイム同期の段階導入。サーバー側通知面を独立させる

- [x] Step 2. メモ保存時に他クライアントへ更新通知を送る
  - 完了時刻: 20:40
  - ブランチ: `docs/move-memo-sync-tasks`（記録のみ、実装は PR #54 関連）
  - PR: #54 関連
  - 変更内容: `PUT /api/memo` 成功時に保存した対象ファイルのメモ更新を broadcast
  - 理由: 複数タブ・複数クライアントでメモ内容を同期

- [x] Step 3. クライアント側でメモ更新通知を受信して現在表示中のメモへ反映する
  - 完了時刻: 20:40
  - ブランチ: `docs/move-memo-sync-tasks`（記録のみ、実装は PR #54 関連）
  - PR: #54（"focus中に保留した古いメモ更新をローカル編集開始時に破棄" 等を含む）
  - 変更内容: `src/template/assets/js/websocket.js`, `memo.js` で現在ファイルのメモのみ反映、編集中は上書きを保留して blur 後に適用、stale 更新を破棄
  - 理由: race condition を避け、編集中の競合上書きを防ぐ

- [x] Step 4. メモのリアルタイム同期競合を検証する E2E テストを追加
  - 完了時刻: 20:40
  - ブランチ: `docs/move-memo-sync-tasks`（記録のみ、実装は PR #54 関連）
  - PR: #54（"memo_updateの統合テストとE2Eテストを追加"、"staleなmemo_updateで巻き戻らないE2Eケースを追加"）
  - 変更内容: `tests/e2e/` に複数タブでのメモ同期・編集中上書き回避・stale 更新破棄を検証する E2E ケースを追加
  - 理由: 同期機能は race condition を起こしやすくユニットテストだけでは不足

## 軽微なリファクタ・保守改善

- [x] `WatchEvent::Error(String)` の構造化エラー化（stale: 既に実装済み）
  - ブランチ: `docs/close-stale-watcherror-todo`
  - 達成箇所:
    - `src/watcher/mod.rs:18` で `Error(WatchError)` として構造化済み（`String` ではない）
    - `src/watcher/error.rs` で `WatchError { kind: WatchErrorKind, detail: String }` 実装済み
    - `WatchErrorKind { Init, Notify, ThreadPanic, ... }` enum 定義済み
    - PR #36（`refactor: watcherエラーAPIを構造化`）で移行完了
  - 理由: TODO 記述は過去の状態（`Error(String)`）を指しており、既に構造化エラーへ移行完了。現状が TODO の目指した最終状態に到達しているため stale クローズ

## 型整理

- [x] `RouteTargetRequest` を struct + enum kind パターンへ変換
  - 完了時刻: 23:35
  - ブランチ: `refactor/route-target-request-struct-kind`
  - PR: #66
  - 変更内容:
    - `src/server/files/resolve.rs`: enum（3 バリアント全てが同一 `query_file` を持つ）を struct `{ kind: RouteTargetKind, query_file }` に分離
    - `RouteTargetKind` enum（Page / ApiContent / ApiMemo）を新設
    - `query_file()` getter を単一アクセスに簡素化、`kind()` getter を test 用途として追加
    - `src/server/files/mod.rs`: `RouteTargetKind` を test 用 re-export として追加
    - `src/server/routes.rs`: バリアント直接構築の test assertion を getter 経由に書き換え
  - 理由: 全バリアントで重複していた `query_file` フィールドを構造体側に集約し、バリアント追加時のフィールド書き忘れリスクを排除。factory メソッド（`page` / `api_content` / `api_memo`）のシグネチャは維持され、呼び出し側 10 箇所は変更ゼロ

## 命名整理

- [x] `ResolvedTarget::update` メソッド名を `attach_file_info` に改名
  - 完了時刻: 22:25
  - ブランチ: `refactor/rename-resolved-target-update`
  - PR: #65
  - 変更内容:
    - `src/server/files/resolve.rs:59` のメソッド名を `update` → `attach_file_info` に変更
    - `src/server/files/content.rs:53, 94` の 2 箇所の呼び出し元を更新
  - 理由: 元のメソッド名 `update` は `UpdateMessage` 型および引数名 `update` と衝突して可読性を損ねていた。実体は `update.with_file(self.relative_path.clone())` で「UpdateMessage にファイル情報を付与する」振る舞いのため `attach_file_info` が意図を明示化
  - TODO 記載の修正: 元 TODO の対象ファイルは stale（`src/server/files.rs L69` → 正しくは `src/server/files/resolve.rs:59`、モジュール分割済み）
